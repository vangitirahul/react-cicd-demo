# Vite React App

This project was scaffolded with Vite (React + JavaScript).

Quick start:

```powershell
cd vite-react-app
npm install
npm run dev
```

Scripts:

- `npm run dev`: Start dev server
- `npm run build`: Build production
- `npm run preview`: Preview production build
